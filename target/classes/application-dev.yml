spring:
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/myblog?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC
      username: root
      password: hyh971127
      initial-size: 1
      min-idle: 1
      max-active: 20
      test-on-borrow: true
      # MySQL 8.x: com.mysql.cj.jdbc.Driver
      driver-class-name: com.mysql.cj.jdbc.Driver
  static-locations: ["/templates/","/resources/","/static","/public/"]
mybatis:
    type-aliases-package: com.heyuanhui.blog.myblog.entity
    mapper-locations: classpath:mapper/*.xml
logging:
  level:
    root: info
  file: log/blog-dev.log

server:
  port: 8081










